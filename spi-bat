from battery.battery import Battery
from util import add_years_to_date

class SpindlernBattery(Battery):
def_init_(self,current_date, last_service_date):
self.current_date = currrent_date
self.last_service_date=last_service_date

def needs_service(self):
date_which_battery_should_be_service_by=add_years_to_date(self.last_service_date,2)
if date which-battery_should_be_service_by<self.current_date:
return True
else:
return False
